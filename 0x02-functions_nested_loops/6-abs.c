#include"main.h"

/**
 * _abs - print the absolute value of an integer
 *@n: integer to print absolute value for
 *
 * Return: returns the absolute value
 */
int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
